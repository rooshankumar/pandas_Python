1 - select specific column
2 - filter rows
3 - combine multiple conditions

1 - square brackets
2 - boolean expressions

selecting columns
1 - a series
2 - dataframe mutliple columns of dataframe

column = df["Column_Name"]
subset = df["Column_Name1","Column_Name2", "...."]

filtering rows
boolean indexing

#based on a single conditions
filtered_rows = df[df[Salary] > 50000]

#combine mutliple conditions
filtered_rows = df[df[Salary] > 50000] & (df[df[Salary2] < 80000])